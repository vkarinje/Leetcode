class Solution(object):
    def hammingWeight(self, n):
        """
        :type n: int
        :rtype: int
        """
        count=0
        while n!=0:
            count=count+n%2
            n=n>>1// right shift
        return count

n<<1 left shift

Time:O(logn)
Space:O(n)
