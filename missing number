class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n=len(nums)
        asum=(n*(n+1))//2
        esum=sum(nums)
        return asum-esum

    Time:O(n)
    Space:O(1)
